# MCP server

The optional MCP server will expose AIBT authoring, validation, compilation, simulation, testing, tracing, benchmark, and node-generation capabilities. It depends on public authoring contracts and is never referenced by player runtime assemblies.

The tool surface and safety model are defined in `Documentation~/ai-and-mcp.md`.
