# Code generation

This area will contain the node source generator, analyzers, templates, deterministic registry generation, parameter serializers, runtime dispatch, editor metadata, and node manifests.

Code generation begins after the node lifecycle and authoring contracts are validated by the Phase 1 vertical slice. It must not use runtime reflection as a fallback for player execution.

